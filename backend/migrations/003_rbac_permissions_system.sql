-- RBAC Permissions System Migration
-- SKIPPED: Tables are managed by TypeORM synchronize (DB_SYNCHRONIZE=true).
-- The permissions, roles, role_permissions, and user_permission_overrides tables
-- are created automatically by TypeORM from the entity definitions.
-- Manual SQL here conflicts with the TypeORM schema (different column names).
-- This file is intentionally left as a no-op.
SELECT 1;
